#include<iostream>
#include<map>
#include<vector>
using namespace std;

int main() {
    int n;
    cin >> n;
    string opc;
    while(cin >> opc) {
        if(opc == "deixar") {

        }

        else if(opc == "endur") {
            
        }
    }
}